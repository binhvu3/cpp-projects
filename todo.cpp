#include <vector>
#include <iostream>
#include <fstream>
#include <string>

void showTasks(const std::vector<std::string> &tasks){
  std::cout << "To-Do List: " << std::endl;
  for {
    (int i=0; i + 1 << ". " << tasks[i] << std::endl);
  }
}

int main(){
  std::vector<std::string> tasks;
  std::string task;
  char choice;

  // Load existing tasks from file
  std::ifstream inputFile("tasks.txt")

  return 0;
}
